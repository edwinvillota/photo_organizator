import {StyleSheet} from 'react-native';
import {Pallete} from '../../../../../theme';

export const styles = StyleSheet.create({
  container: {
    borderBottomColor: Pallete.Primary,
    borderBottomWidth: 2,
    paddingVertical: 5,
    marginBottom: 5,
  },
  title: {color: Pallete.Primary},
});
